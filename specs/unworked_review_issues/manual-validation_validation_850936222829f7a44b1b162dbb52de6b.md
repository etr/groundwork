# Unworked Review Issues

**Run:** 2026-09-14 14:25:07
**Task:** manual-validation
**Total:** 70 (0 critical, 7 major, 63 minor)

## Major

1. [ ] **code-quality-reviewer** | `bin/groundwork-run.js:816` | dead-or-misleading-validation
   In phaseChildLeases, `sameLeaseIdentity(owner, owner)` compares the object to itself, which is always true for any truthy owner (every field comparison is x === x), so the 'Phase child lease identity is invalid' guard can never fire and the owner-shape validation it implies (token format, pid, taskId, startedAt) is absent. No test exercises this guard, consistent with it being unreachable as written.
   *Recommendation:* Either validate the owner fields for real (reuse the field predicate from inspectLease/sameLeaseIdentity against a normalized expectation, e.g. normalizeLeaseOwner(owner) round-trip or the 48-hex token check) or drop the pretense and rely on the documented internal construction of release.record, removing the dead branch.

2. [ ] **code-quality-reviewer** | `lib/persist-unworked-findings.js:102` | error-handling
   shouldPersist treats any unrecognized non-empty disposition as non-actionable: only exactly 'actionable' (case-insensitive) survives, so a typo'd or future protocol disposition value (e.g. 'actionble', 'pending') is silently dropped from the durable unworked ledger. This is the one input path in the change that degrades silently rather than failing closed, and it can lose real findings on producer/consumer protocol drift.
   *Recommendation:* Fail closed on unrecognized disposition values (throw naming the offending value and finding id) or default unknown values to persisted-and-flagged; add a test for an unrecognized disposition string.

3. [ ] **conventions-reviewer** | `skills/swarm-debug/SKILL.md:232` | code-pattern
   The changed 'Debug Journal — Swarm Extension' section instructs: "Use the same invocation-unique `{{debug_dir}}/${SLUG}-<timestamp>-<pid>.md` journal format as the debug skill" — a flat timestamp+PID uniqueness contract. docs/developing-skills.md → Path Safety invariant 1 (mirrored in CLAUDE.md/AGENTS.md 'Path safety') explicitly prohibits exactly this for journals: "Never a fixed slug-only name, never a timestamp+PID-only contract (same-second and PID-reuse collisions), never a shared `current` pointer" — the required contract is an atomically unique mktemp run directory (`RUN_DIR="$(mktemp -d "{{debug_dir}}/${SLUG}-XXXXXX")"; JOURNAL_PATH="$RUN_DIR/journal.md"`). The line also misstates the debug skill's actual format (a journal.md inside a run directory, not a flat .md). The branch's own Phase 1 text in this same file (line ~85) and the diagram (line 76, `{{debug_dir}}/{slug}-xxxxxx/journal.md`) prescribe the correct contract, so the file now teaches two contradictory journal shapes; tests/path-safety.test.js passes only because it keys on the correct contract elsewhere in the file. The base line was `{{debug_dir}}/{slug}.md`, so this prohibited spelling was actively introduced by this branch.
   *Recommendation:* Reword line 232 to the run-directory contract, e.g.: "Use the same invocation-unique run-directory journal as the debug skill — the literal `$RUN_DIR/journal.md` from the Phase 1 `mktemp -d "{{debug_dir}}/${SLUG}-XXXXXX"` allocation — with an additional `## Swarm Evidence` section:". Consider extending tests/path-safety.test.js's journal scan to reject `<timestamp>-<pid>` spellings so a contradictory section cannot coexist with a compliant one.

4. [ ] **housekeeper** | `README.md:538` | documentation-stale
   README.md:538 states "Startup reads, model phases, and repair phases use a writer-preferred repository reader gate," which the repair delta's narrowed rule in specs/architecture.md:5 ("Model phases (plan, implement, validate, finalize) deliberately do not hold the reader gate") and :27 ("ordinary plan, implement, validate, and finalize calls carry only the per-task project lease") now directly contradict. The claim was consistent with the pre-repair spec (HEAD's "token-named reader leases admit concurrent model phases" / "gives phase agents reader access") and became false only after the repair narrowed the behavior in code and spec without updating the user-facing README concurrency paragraph. A reader of README would expect a publication in another package to wait out a running phase — exactly what the repaired spec says must not happen.
   *Recommendation:* Update the README concurrency paragraph to state that model phases run under the per-task project lease alone, while the repository gate is taken only at short lifecycle call sites (startup checks, plan-ignore setup, workspace preparation and the worktree registry, completed-task cleanup, publication), matching specs/architecture.md:5,9,27.

5. [ ] **housekeeper** | `skills/swarm-debug/SKILL.md:232` | documentation-stale
   The Swarm Extension section still teaches the intermediate journal design this change abandoned: "Use the same invocation-unique `{{debug_dir}}/${SLUG}-<timestamp>-<pid>.md` journal format as the debug skill". This contradicts the final mktemp-based design everywhere else in the same change — swarm-debug's own diagram (line 76: `{{debug_dir}}/{slug}-xxxxxx/journal.md`) and Phase 1 (line 85: `RUN_DIR="$(mktemp -d "{{debug_dir}}/${SLUG}-XXXXXX")"; JOURNAL_PATH="$RUN_DIR/journal.md"`), the debug skill's journal discipline it claims to mirror, and docs/developing-skills.md invariant 1 which explicitly prohibits it ("never a timestamp+PID-only contract (same-second and PID-reuse collisions)"). The path-safety test's pid check (regex `/\$\$[-.)]/` at tests/path-safety.test.js:419) only catches shell `$$PID` spellings, so this prose `<pid>` spelling slipped through. An agent following line 232 would write its Swarm Evidence journal to a different, collision-prone path than the one Phase 1 of the same skill allocated.
   *Recommendation:* Rewrite line 232 to reference the invocation-unique run-directory journal Phase 1 creates (e.g. "Use the same invocation-unique `{{debug_dir}}/${SLUG}-XXXXXX/journal.md` journal you allocated in Phase 1, with an additional `## Swarm Evidence` section"), matching skills/debug/SKILL.md and the invariant-1 journal contract in docs/developing-skills.md.

6. [ ] **housekeeper** | `specs/product_specs.md:39` | spec-not-updated
   The recovery resync edited only line 37; two old-writer-gated-model fragments survive in the same spec and now contradict it. (a) Line 39 still reads "Accepted recovery changes SHALL be regular selected-project files, staged only from an independently revalidated path set, and sealed by the runner in a recovery commit" — but line 37 (edited in this pass) says "The runner SHALL NOT create preservation or rescue snapshots, police repository boundaries, or restore rejected mutations around a recovery session. Repair changes SHALL remain in the selected task worktree for the retried phase and its normal commit flow", specs/architecture.md:27 says "Recovery creates no snapshots, rollback transaction, special commit, or repository-wide boundary comparison", committed docs/getting-started.md:80 and README.md:526 say there are no recovery-only commits, and bin/groundwork-run.js contains no recovery-seal/recovery-commit/revalidated-path-set machinery (only validation status snapshots and preserved-workspace diagnostics). (b) Line 7 still enumerates recovery among "Repository-wide mutations—including worktree creation/removal, recovery, merge publication, and branch cleanup—SHALL be exclusive", asserting repository-wide exclusivity for a session line 37 now places under the per-task project lease with no repository gate. Before this pass, lines 7 and 39 were consistent with the then-current line 37 (old transaction model); the line-37 rewrite is what made the section internally contradictory, so the resync is incomplete rather than absent.
   *Recommendation:* Finish the product-spec sweep in one edit: remove "recovery" from the repository-wide-mutations list at specs/product_specs.md:7 (it is a worktree-local repair session under the project lease), and rewrite the first sentence at :39 to drop "staged only from an independently revalidated path set" and "sealed by the runner in a recovery commit" (e.g. repair changes remain in the worktree for the retried phase and its normal runner-owned commit flow; any observed finalization-phase mutation still invalidates prior validation per line 35). Run /groundwork-source-product-specs-from-code if a fuller pass is preferred.

7. [ ] **security-reviewer** | `lib/persist-unworked-findings.js:219` | sensitive-data-exposure
   Reviewer finding text (finding.finding / finding.recommendation) is rendered raw into the committed specs/unworked_review_issues/*.md ledger with no redaction. Security reviewers routinely quote the exact secret-bearing code they flag (API keys, tokens, private keys), so a run that surfaces a hardcoded secret persists that secret verbatim into a file that is committed with the worktree and can later be pushed. The runner already has a redaction pass for exactly this class of content (redactRecoveryText in bin/groundwork-run.js:2810) but the unworked-findings writer applies none.
   *Recommendation:* Apply a shared redaction helper (reuse redactRecoveryText's key/credential patterns) to finding and recommendation text in renderMarkdown before writing the ledger artifact, and add a test asserting a quoted sk-/AKIA/ghp_/PRIVATE KEY string never reaches the written markdown.

## Minor

8. [ ] **architecture-alignment-checker** | `bin/groundwork-run.js:1093` | pattern-violation
   activeProjectOwners reads each project lease via readLiveLease(file, dependencies) without the sharded mutationRoot override, so a stale project lease encountered through this exported function would be reaped by reclaimStaleLease under the default mutationRoot path.dirname(leasePath) = groundwork/projects/ — the legacy shared queue location — contradicting the amended architecture.md synchronization paragraph ('Every lease transition — publication, stale reap, gate admission — runs inside a serialized mutation turn: a ticket queue in groundwork/projects/<projectKey>.queue/') and able to write live records into projects/.lease-mutation that would spuriously trip the new requireDrainedLegacyProjectQueue fail-closed errors. Mitigating: the function has no internal caller in the runner (export surface and tests only), so no shipped flow reaches it; the gap was left by the branch's sharding commit (6de36c0), not by this repair.
   *Recommendation:* In activeProjectOwners, compute the per-holder sharded queue root the way acquireProjectLease does (sha256(holder.project).slice(0,32) + '.queue') and pass it via { ...dependencies, mutationRoot } to readLiveLease, so any future caller reaps on the same queue the acquirers use.

9. [ ] **architecture-alignment-checker** | `hooks/pin-session-selection.sh:25` | technology-choice
   The PostToolUse pin hook now hard-requires jq (clean no-op when absent, by design), introducing a new soft external dependency, but the documented dependency list in AGENTS.md/CLAUDE.md ('External Dependencies': git, gh, node, bash 3.2+) was not updated, so contributors cannot discover that per-chat selection pinning silently degrades without jq.
   *Recommendation:* Add jq to the External Dependencies sections of AGENTS.md and CLAUDE.md, noting it is optional and that the pin hook no-ops without it.

10. [ ] **architecture-alignment-checker** | `pi-extension/lib/project-context-core.js:33` | pattern-violation
   A second hand-maintained copy of the strict .groundwork.yml grammar now exists (pi-extension copy vs lib/project-context.js parseConfigResult). lib/project-context.js's own comment justifies the exact grammar on the grounds that 'a broken monorepo config could be interpreted differently by each parser that tolerates a different subset of YAML', yet tests/pi-extension.test.js validates the Pi copy only against hand-written expectations — there is no parity test feeding the same config corpus through both parsers, so the two grammars can drift silently between the plugin runtime and the Pi extension.
   *Recommendation:* Add a corpus parity test (tests/pi-extension.test.js or tests/project-context.test.js) that runs an identical set of valid/invalid .groundwork.yml fixtures through lib/project-context.js parseConfigResult and pi-extension/lib/project-context-core.js parseGroundworkYmlResult and asserts identical classifications.

11. [ ] **architecture-alignment-checker** | `skills/swarm-debug/SKILL.md:232` | pattern-violation
   The 'Debug Journal — Swarm Extension' section prescribes `{{debug_dir}}/${SLUG}-<timestamp>-<pid>.md`, contradicting the invocation-unique run-directory convention (`mktemp -d "{{debug_dir}}/${SLUG}-XXXXXX"` containing journal.md) that this same change installs in skills/debug/SKILL.md and in swarm-debug's own Phase 1 text (line 82) — one skill now teaches two different journal shapes for the same artifact, and the <timestamp>-<pid> suffix form is exactly the fixed-name pattern the path-safety rework replaces.
   *Recommendation:* Rewrite line 232 to reference the run-directory form ('{{debug_dir}}/${SLUG}-<unique>/journal.md as allocated in Phase 1') so both sections teach one shape.

12. [ ] **code-quality-reviewer** | `bin/groundwork-run.js:1439` | consistency
   saveCheckpoint reimplements the tmp+fsync+rename pattern with a pid+Date.now() temporary name, diverging from lib/atomic-write.js's documented cryptographically-unique temporary invariant ('a crashed writer's leftover temporary can never collide with a later writer'); two saves in the same millisecond from the same process would crash with an unhandled EEXIST. The manifested atomic-write helper is already available via requireRuntimeHelper.
   *Recommendation:* Replace the hand-rolled body with writeJsonSyncAtomic (or temporaryName) from the manifested atomic-write helper so the runner's own checkpoint state follows the same atomic-write contract it helped define.

13. [ ] **code-quality-reviewer** | `bin/groundwork-run.js:2153` | duplicated-divergent-logic
   The PHASE_CHILD_SUPERVISOR heredoc embeds a copy of processStartIdentity that diverges from lib/process-identity.js (no zombie check; kill-ESRCH throws 'Cannot identify phase child process instance' instead of returning null), while the comment at line 620 declares process-identity.js the 'single process-identity authority'. The duplication is forced by the /bin/sh exec environment, but the divergence is undocumented at the copy site.
   *Recommendation:* Add a one-line note in the embedded copy stating it deliberately omits the zombie probe and maps ESRCH to hard failure (with the consequence: a parent that dies in the startup window aborts the phase child), so the two implementations cannot drift silently.

14. [ ] **code-quality-reviewer** | `bin/groundwork-run.js:3393` | misleading-comment
   classifyExistingWorktree carries a dead assignment under a comment that misstates the mechanism: 'Assign at the exact point the inline code did, so a mid-classify failure still reports the preserved worktree in the task error' precedes `implementation = existing;`, but that assigns the destructured local parameter, which is never read before the return (the return uses `existing` directly). The outer `implementation` is assigned only after the function returns, so a mid-classify throw still escapes with the outer variable null; worktree-preservation reporting in the task-level catch is actually covered by `preservedWorkspace` (set at line 3552 before classification runs). No user-visible regression today, but the comment invites a future editor to remove the preservedWorkspace fallback believing this assignment covers it.
   *Recommendation:* Delete the dead `implementation = existing;` assignment and its comment; if a locator is wanted, one plain line noting that mid-classify failures are reported via preservedWorkspace.

15. [ ] **code-quality-reviewer** | `lib/lease-mutation.js:222` | operational-robustness
   mutationEntries throws on any filename in .lease-mutation/{tickets,choosing} that is not a 48-hex .lock entry, so a single stray file (.DS_Store, editor temp file) permanently wedges every future mutation turn for that queue until manual removal. Fail-closed is the declared posture and staging files do self-heal, but foreign files have no recovery path and the resulting error gives no remediation hint beyond the path.
   *Recommendation:* Either skip a narrow set of known-benign foreign names (dotfiles not matching the staging pattern) with the queue intact, or extend the error message to state the operator remedy ('remove the file to unblock the queue') — documenting the wedge/recovery contract where the throw happens.

16. [ ] **code-quality-reviewer** | `lib/owned-lock.js:194` | edge-case
   `const staleMs = options.staleMs || LOCK_STALE_MS` silently ignores an explicit staleMs of 0, which in the module's semantics should mean 'every unverifiable lock is immediately reapable' (a legitimate calibration value for tests or aggressive reclamation); a falsy-coercion default defeats the documented option.
   *Recommendation:* Use nullish coalescing: `options.staleMs ?? LOCK_STALE_MS`.

17. [ ] **code-quality-reviewer** | `lib/process-identity.js:51` | edge-case
   The Linux /proc/<pid>/stat reader splits on whitespace without anchoring after the closing paren of the comm field (the standard technique), so a process name containing spaces shifts the field indices; if field 22's position then happens to hold a numeric value, the wrong starttime is recorded, which would later mark a live holder as dead and reap a live lock.
   *Recommendation:* Parse from the substring after the last ')' (or validate that field 1 equals the requested pid before trusting field 22); the ps fallback already covers most hosts, so this only tightens the Linux fast path.

18. [ ] **code-quality-reviewer** | `lib/process-identity.js:59` | test-coverage
   The repair's new internals — self-identity memoization (lines 62-64), zombie folding in the /proc path (line 87), and the combined `ps -o lstart=,stat=` parse with recovered-spelling lstart (lines 90-100) — have no direct tests; the injected dependencies.processStartIdentity seam replaces the whole function, so the internals are only exercised incidentally (validation-session and groundwork-run suites on macOS). Verified this run: the recovered lstart spelling is byte-identical to the legacy `ps -o lstart=` output on darwin, so record compatibility holds, but nothing pins it.
   *Recommendation:* Add a focused suite pinning: memoization returns the same self-identity across calls without re-spawning ps; the recovered lstart equals the legacy `ps -o lstart=` spelling; zombie-state outputs classify as dead where the platform allows stubbing.

19. [ ] **code-quality-reviewer** | `lib/project-context.js:312` | readability
   loadConfig's JSDoc block ('Fail-closed contract: a MISSING config is a valid single-project repository...') is orphaned: it is immediately followed by validateProjectMapping's own JSDoc and function (lines 323-336), so the loadConfig contract visually documents the wrong function and loadConfig itself (line 396) has no doc at the definition site.
   *Recommendation:* Move the loadConfig doc block down to directly precede function loadConfig so each doc sits on its own function.

20. [ ] **code-quality-reviewer** | `lib/project-context.js:651` | documentation-drift
   getPaneKey's doc comment lists a four-step fallback chain ending with 'session-<sessionId> (last resort)', but the code has a fifth pid-keyed fallback (line 738-741) and the inline comments number the repo hash as 'Fallback 2' and session as 'Fallback 3' while the doc numbers them 3 and 4 — three inconsistent numberings of the same chain.
   *Recommendation:* Update the doc list to include the pid last resort and align the inline fallback numbering with it (or drop numbers from the inline comments and keep one authoritative ordered list).

21. [ ] **code-quality-reviewer** | `lib/validation-session.js:1356` | dead-code
   The owner_token field in cliResult is always null: main() nulls opened.ownerToken in every branch that could carry a token (both runner-mode and manual paths persist a capability file first), so the adjacent comment 'the raw token itself appears only when no file could be persisted' describes an unreachable path — there is no code path where persistence fails yet the token is still emitted.
   *Recommendation:* Either remove the owner_token field from the CLI result (callers now consume capability_file only) or make the comment true by actually handling a saveManualCapability failure distinctly instead of letting it propagate.

22. [ ] **code-quality-reviewer** | `tests/run-tests.sh:48` | error-handling
   The watchdog disarm kills the watchdog's sleep child before TERMing the watchdog itself. Killing the sleep wakes the watchdog subshell, which then proceeds into `pkill -KILL -P "$suite_pid"` and `kill -9 "$suite_pid"` for a suite that already finished — a tiny pid-reuse window where kill -9 can hit an unrelated recycled pid. Also the `suite_status -gt 128` hint prints '(watchdog killed ...)' for any signal exit (e.g. a native crash at 139), not just watchdog kills. Bash 3.2 compatibility itself is verified clean (/bin/bash -n passes).
   *Recommendation:* TERM only the watchdog pid in the disarm (an orphaned sleep is harmless); soften the >128 message to 'exited by signal' or verify timeout attribution before claiming it.

23. [ ] **code-simplifier** | `bin/groundwork-run.js:1215` | dead-code
   In acquireRepositoryGate's write path, releaseIntent() is wrapped in `try { ... } catch (error) { throw error; }` — a catch that only rethrows, adding nesting without any behavior. The surrounding code already uses bare releaseIntent() in its own catch at line 1231.
   *Recommendation:* Remove the try/catch and call releaseIntent() directly, matching line 1231's style.

24. [ ] **code-simplifier** | `bin/groundwork-run.js:2148` | duplication
   The PHASE_CHILD_SUPERVISOR shell template embeds its own re-implementations of processStartIdentity (without the zombie check lib/process-identity.js performs) and sameParent (verbatim semantic copy of sameLeaseIdentity at line 743). These copies cannot require the lib (they run inside /bin/sh -c node -e), so they are constrained — but nothing records that the duplication is deliberate, and the liveness semantics can drift from the module both are named after (zombie handling already differs).
   *Recommendation:* Either inject the manifested helper's absolute path via an environment variable and require it in the supervisor script, or add a one-line comment at PHASE_CHILD_SUPERVISOR stating the duplication is deliberate and which lib modules must be kept in sync (process-identity.js, sameLeaseIdentity), so the external-runner-manifest closure check plus the note guard the drift.

25. [ ] **code-simplifier** | `bin/groundwork-run.js:3360` | code-structure
   The repair extracted the four stages as helpers nested inside runTasks rather than hoisted module-level functions, so runTasks still spans ~1050 lines and the helpers close over substantial outer state (taskEnv, taskProjectRoot, taskSpecsDir, lexicalTaskProject, beginPhase/completePhase, taskLog, repoRoot/projectRoot/taskId/options, activePhase/activeRepairInput). The stage boundaries, explicit input/output lists, and the honest capture comment are a real readability improvement over the inline blocks, and hoisting further would risk behavior in this concurrency-critical path — but the Rule-of-500 smell flagged in iter-1 is structurally deferred, not eliminated.
   *Recommendation:* Acceptable to close as-is given the behavior-preservation constraints. If touched again, the next seam is hoisting runFinalize/publishOrRetry to module level by parameterizing the phase/report closures — do it behind the existing groundwork-run tests, which already pin the observable contract (183 passing).

26. [ ] **code-simplifier** | `bin/groundwork-run.js:3395` | code-structure
   Inside classifyExistingWorktree, 'implementation = existing;' assigns the destructured parameter, which shadows the outer loop variable; the local is never read afterwards (the body and the return use 'existing'), and the preceding comment claims this assignment is what makes a mid-classify failure 'still report the preserved worktree in the task error' — mechanically false. Observable behavior is preserved only because the outer catch's 'implementation || preservedWorkspace' falls back to preservedWorkspace, which was set earlier from the same canonical worktree path and branch. A future edit that relies on the comment (e.g., removing the preservedWorkspace assignment, or reading outer implementation after a classify throw) would silently change the error contract.
   *Recommendation:* Delete the dead assignment and rewrite the comment to name the real mechanism: the outer catch reports preservedWorkspace (same canonical path and branch), which is what preserves the message. Alternatively pass no implementation parameter and return { implementation: null, ... } in the !branchExists branch.

27. [ ] **code-simplifier** | `bin/groundwork-run.js:3604` | duplication
   The guard for inspecting an active validation session (lines 3604-3608) repeats the exact five-term conjunction (worktreeClean + planSha256 + branch + worktreePath + taskHead) that was just computed into implementationMatches at lines 3593-3598. The duplication invites the classic bug where one copy is later updated (e.g. a new checkpoint field) and the other is not, silently changing which sessions are resumable.
   *Recommendation:* Replace the inline conjunction with the already-computed boolean: `if (!acceptedIntegration && inspectValidation && implementationMatches) {` — behavior-identical since implementationMatches is the same conjunction including worktreeClean and the checkpoint.implementation truthiness check.

28. [ ] **code-simplifier** | `bin/groundwork-run.js:606` | naming
   waitForLease (line 606, sleeps 1000ms while another process holds a lease) and waitForLeaseMutation (line 613, sleeps 10ms between mutation-queue retries) differ by five characters and one letter of purpose but have 100x different defaults and distinct roles in the lock protocol; a reader must inspect the defaults to tell the heartbeat wait from the retry backoff apart.
   *Recommendation:* Rename to waitForLeaseHeartbeat (or waitForLeaseHolder) and waitForMutationRetry respectively, keeping the dependency seams (wait / mutationWait) unchanged.

29. [ ] **code-simplifier** | `bin/groundwork-run.js:816` | dead-code
   phaseChildLeases validates each entry with sameLeaseIdentity(owner, owner) — comparing the object to itself, which is true for every truthy owner. The check reduces to !leasePath || !owner, so the call suggests an owner-shape verification that does not actually happen, and a reader must reconstruct why a self-comparison exists (there is no reason; it verifies nothing).
   *Recommendation:* Delete the sameLeaseIdentity(owner, owner) term, keeping `if (!leasePath || !owner)`. If shape validation of the owner record was intended, validate the fields explicitly (version/pid/token/taskId presence) instead of a self-comparison.

30. [ ] **code-simplifier** | `hooks/session-start.sh:231` | duplication
   The template-variable block (the exact seven `- {{var}} = value` lines) is built twice: once inside session-start.sh's inline node -e (lines 231-243) and once in lib/resolve-template-vars.js (lines 23-39). Adding a template variable (as this change did for the absolute-path contract) requires editing both; a miss makes the SessionStart context and the PostToolUse context silently disagree about available bindings.
   *Recommendation:* Refactor lib/resolve-template-vars.js to export a templateVarLines() function (keeping the stdin hook entry intact), and have session-start.sh's inline node call `console.log(require('.../resolve-template-vars').templateVarLines().join('\n'))` so the variable list has one source.

31. [ ] **code-simplifier** | `lib/lease-mutation.js:32` | duplication
   The path-containment predicate is implemented five times across the changed code: isContained in lib/lease-mutation.js:32 and bin/groundwork-run.js:586 (identical, equality-allowing), isContained in lib/plan-check.js:57, lib/project-context-cli.js:61, and isContainedBy in lib/project-context.js:989 (path.resolve-normalizing variants), plus the pairwise strictlyNested spelling twice more inside the two validateProjectMapping copies. The copies already disagree subtly (some normalize inputs, one allows relative === ''), which is exactly how containment bugs creep in for a path-safety invariant this task froze.
   *Recommendation:* Export one canonical isPathContained(parent, child) (resolve-normalizing, equality-allowing, matching the lease-mutation semantics) from lib/utils.js and replace the five local copies; the two strictlyNested variants can then be `relative === '' || isPathContained(...)`. The pi-extension core copy stays (self-contained by design) but should call the same spelling internally.

32. [ ] **code-simplifier** | `lib/validation-session.js:1349` | dead-code
   cliResult emits `unworked_artifact: opened.state.unworkedArtifact || null`, but no writer in the repository ever sets state.unworkedArtifact (repo-wide grep finds only this line), so the field is constant null in every open result. A consumer reading it cannot learn anything, and a maintainer will reasonably assume some path populates it.
   *Recommendation:* Either remove the key from cliResult (verify the validate skill/coordinator JSON parsing tolerates absence first — since it has always been null, any consumer must already handle null/absent), or wire it to the real unworked-artifact path the complete/abandon flows produce, so the advertised field carries data.

33. [ ] **code-simplifier** | `lib/validation-session.js:506` | documentation
   Orphaned JSDoc: the block at 506-514 documents withRunLock's serialization contract, but withRunLock is defined at line 546 — between them sit a second JSDoc (515-520, for readSessionStateLightweight at 533) plus isContentionError and heartbeatExitError. The first doc appears to document isContentionError. The same detached pattern exists in lib/project-context.js lines 312-322, where loadConfig's fail-closed JSDoc is immediately followed by validateProjectMapping's JSDoc (323-335) while loadConfig sits at 396 undocumented. Both detached docs describe load-bearing concurrency/fail-closed contracts, so misattachment is actively misleading.
   *Recommendation:* Move each JSDoc block onto the function it documents (withRunLock at validation-session.js:546; loadConfig at project-context.js:396), leaving readSessionStateLightweight's and validateProjectMapping's docs where they are.

34. [ ] **code-simplifier** | `lib/validation-session.js:546` | duplication
   withRunLock (546-570), withHeartbeatLock (579-609), and withOpenStateMutation (762-781) all end with the identical commit tail — run mutate(loaded), spread, revision: loaded.state.revision + 1, updatedAt: new Date().toISOString(), atomicWriteJson(loaded.stateFile, state), release in finally. The revision/updatedAt stamping protocol for durable session writes is therefore maintained in three places; a change to the stamping (e.g. also bumping a monotonic counter) must be applied three times.
   *Recommendation:* Extract commitLoadedState(loaded, mutate) that performs the mutate/spread/revision/updatedAt/atomicWriteJson steps and returns the new state; each wrapper keeps its own lock acquisition and load strategy (full loadSession vs readSessionStateLightweight) but delegates the commit tail. Behavior-identical.

35. [ ] **code-simplifier** | `lib/validation-session.js:99` | naming
   The run-directory name pattern /^groundwork-validation-[a-f0-9]{32}$/ is written out three times (lines 93, 389, 535), the name is constructed from a bare template literal at line 897, and loadSession extracts the runId with the magic number slice(22) (the length of 'groundwork-validation-'). A future rename of the prefix requires four coordinated edits, and 22 is unexplained at the use site.
   *Recommendation:* Hoist RUN_NAME_PREFIX = 'groundwork-validation-' and RUN_NAME_PATTERN = new RegExp(`^${RUN_NAME_PREFIX}[a-f0-9]{32}$`) next to SESSION_SCHEMA_VERSION, use the pattern at all three validation sites, and derive the runId as basename.slice(RUN_NAME_PREFIX.length).

36. [ ] **conventions-reviewer** | `CLAUDE.md:91` | project-structure
   The 'Hook Events' section's PostToolUse bullet still reads "(`check-commit-alignment.sh`, `resolve-template-vars.js`): Validates commit/spec alignment; resolves `{{template}}` vars" — it omits pin-session-selection.sh even though hooks/hooks.json's PostToolUse/Bash entry runs it and this branch materially reworked its contract to receipt-based pinning (the hooks.json description was updated to "pin verified project-selection receipts into the per-chat snapshot", but the convention documents were not). The same omission exists at AGENTS.md line 91. The listing gap predates the branch, but the branch's selection-pinning deliverable makes it materially stale and both files were edited in this branch without closing it.
   *Recommendation:* Add pin-session-selection.sh to the PostToolUse bullet in both CLAUDE.md and AGENTS.md, e.g.: "- **PostToolUse** (`check-commit-alignment.sh`, `pin-session-selection.sh`, `resolve-template-vars.js`): Validates commit/spec alignment; pins verified project-selection receipts into the per-chat snapshot; resolves `{{template}}` vars".

37. [ ] **conventions-reviewer** | `hooks/pin-session-selection.sh:25` | tool-usage
   The reworked pin hook makes jq a hard gate (`command -v jq > /dev/null 2>&1 || exit 0`; the previous sed fallbacks for `tool_input.command` and `session_id` were removed), so the per-chat selection pinning — a headline deliverable of this task — silently no-ops on machines without jq. Yet the 'External Dependencies' section of CLAUDE.md/AGENTS.md (both edited by this branch for the lib table and path-safety paragraph) still lists only git, gh, node, and bash, and does not document jq or its no-op degradation.
   *Recommendation:* Add `jq` to the External Dependencies section of both CLAUDE.md and AGENTS.md, noting that the selection-pinning hook (and other hooks) degrade to a safe no-op without it.

38. [ ] **housekeeper** | `AGENTS.md:91` | documentation-stale
   The Hook Events section lists PostToolUse as only "(`check-commit-alignment.sh`, `resolve-template-vars.js`)" but omits `pin-session-selection.sh`, the third PostToolUse hook (Bash matcher) that this branch modified (59 lines) and whose hooks.json description was updated here to "pin verified project-selection receipts into the per-chat snapshot". The omission predates the branch (the hook also existed at base SHA 8a1c5f3), but monorepo selection pinning is headline scope of this task and the branch updated every surrounding doc (select-project SKILL.md, README) while leaving this canonical contributor enumeration without the hook or its selection-pinning role (the same stale line is mirrored at CLAUDE.md:91).
   *Recommendation:* Add `pin-session-selection.sh` to the PostToolUse bullet in AGENTS.md and CLAUDE.md (e.g. "PostToolUse (`check-commit-alignment.sh`, `pin-session-selection.sh`, `resolve-template-vars.js`): validates commit/spec alignment; pins verified project-selection receipts; resolves `{{template}}` vars").

39. [ ] **housekeeper** | `README.md:540` | documentation-stale
   README.md's upgrade paragraph tells operators only that "v2 rejects a detected live `groundwork/runner.lock`," while the repair added a second drain trigger documented at specs/architecture.md:41: the runner fails closed before acquiring a project lease if the legacy shared queue `groundwork/projects/.lease-mutation` holds records, with a distinct manual-remediation step for stale records (remove the drained queue directory manually) that is not derivable from the generic stop-and-drain instruction. The existing operator guidance remains correct, so this is a detail gap, not a falsehood.
   *Recommendation:* Extend the README upgrade paragraph to mention that v2 also rejects live or stale records in the legacy pre-sharding queue `groundwork/projects/.lease-mutation`, and that stale records require manual removal after confirming no pre-sharding runner remains.

40. [ ] **housekeeper** | `specs/architecture.md:7` | documentation-stale
   The repaired sentence enumerates "Every lease transition — publication, stale reap, gate admission — runs inside a serialized mutation turn: a ticket queue in `groundwork/projects/<projectKey>.queue/` beside that project's lease," but gate (and workspace-registry) transitions do not queue there: acquireRepositoryGate/acquireWorkspaceRegistryLease override mutationRoot to the gate root (bin/groundwork-run.js:1160, `mutationRoot: root`), so they serialize at `groundwork/repository-gate/.lease-mutation/`, a location the architecture doc never names. The scoping cues ("beside that project's lease", "sharded per project") limit the damage, but the enumeration invites reading gate admission as queued on the per-project queue.
   *Recommendation:* Scope the enumeration in specs/architecture.md:7 to project-lease transitions, and add one clause noting that repository-gate and workspace-registry transitions serialize in the gate's own mutation queue under `groundwork/repository-gate/`.

41. [ ] **housekeeper** | `specs/architecture.md:9` | documentation-stale
   Leftover pre-narrowing phrasing survives inside and beside the repaired text: specs/architecture.md:9's tail says cleanliness scans occur at writer-gated boundaries "rather than before and after every reader phase" (inside a paragraph the repair rewrote to say "a phase never holds it"), and :39 says v2 avoids runner.lock "which preserves concurrent v2 reader phases." Both use "reader phase" — terminology from the old model in which phases held reader leases — contradicting the new rule at :5/:27 that phases carry only the per-task project lease.
   *Recommendation:* Reword :9 to "rather than before and after every phase" and :39 to "preserves concurrent v2 readers" (or "concurrent v2 phases") so no paragraph implies phases hold the reader gate.

42. [ ] **performance-reviewer** | `bin/groundwork-run.js:1213` | n-plus-one
   The repository-gate read loop scans waitingWriters twice per poll iteration (line 1173 and again at 1188 for the double-check) and liveLeaseFiles opens+probes every lease file per scan; inspectLease (line 684) pays a processStartIdentity probe (2 ps spawns on macOS) per live holder per scan. Under gate contention at the 1s poll interval, k live waiting writers cost ~4k ps spawns per second, all re-probing the same immutable (pid, processStart) pairs; activeProjectOwners (line 1092) has the same per-file probe pattern per status call.
   *Recommendation:* Scan waitingWriters once per iteration and reuse the result for the post-acquire double-check (only the writer.lock existence needs re-reading), and cache liveness verdicts keyed by (pid, processStart) for one poll interval — identity cannot legitimately change within a second, and a recycled pid produces a different processStart anyway.

43. [ ] **performance-reviewer** | `bin/groundwork-run.js:625` | missing-caching
   The runner's processStartIdentity wrapper calls requireRuntimeHelper('process-identity.js') on every probe, and requireRuntimeHelper re-runs fs.existsSync on its candidate directories (1-2 stats per call) before hitting Node's require cache. This pattern predates the repair delta (the wrapper's lines are unchanged context in the diff), and the stat cost is negligible relative to the ps spawn it precedes on foreign-pid probes, but it is the only repeated-fs-stat pattern on the probe paths examined under closure question 3.
   *Recommendation:* Non-blocking. If touched again, hoist the resolved module to a module-level const (e.g. const processIdentity = requireRuntimeHelper('process-identity.js')) so per-probe directory stats disappear; self-pid probes are already amortized by the new memoization.

44. [ ] **performance-reviewer** | `lib/lease-mutation.js:125` | blocking-io
   Every ephemeral queue-record publish fsyncs the record file (line 97) and directory-fsyncs TWICE (after the link at line 108 and again after the staging unlink at line 125) — ~6 fsyncs per mutation turn for choosing/ticket records that are unlinked moments later at turn end. Crash-consistency for these records is already recovered without them: vanished entries are treated as released (inspectMutationEntry ENOENT -> null) and dead mutators are reclaimed by liveness. This pattern pre-existed in the runner, but the change multiplies its application: every owned-lock acquire AND release now takes a full turn.
   *Recommendation:* For queue-internal records (choosing/ticket), drop the second directory fsync after staging cleanup (line 125) — durability is only load-bearing for the published target record, not the ephemeral queue — or gate both directory fsyncs behind an explicit durability flag so turn cost halves on hot paths while the runner's durable lease publications keep full fsync discipline.

45. [ ] **performance-reviewer** | `lib/lease-mutation.js:288` | algorithmic-complexity
   acquireLeaseMutation scans the tickets directory twice (line 288 to pick the ticket number, line 300 to find predecessors) and the choosing directory once (line 294) per turn; each mutationEntries scan opens+reads+validates every queue entry (inspectMutationEntry), and reclaimStaleStagingEntries additionally readdirs+lstats both directories every turn. With k concurrent waiters that is 3*O(k) file opens per turn and O(k^2) aggregate per queue drain. k is small in practice (concurrent mutators), so impact is bounded, but the second tickets scan is pure re-read of state that cannot have shrunk the caller's bakery order.
   *Recommendation:* Publish the ticket, then take ONE combined scan of choosing+tickets and compute both the contender set and predecessors from that single snapshot; skip reclaim when readdir shows no `.staging-` entries (it already readdirSyncs before deciding — return early when the listing has no staging names instead of lstatting).

46. [ ] **security-reviewer** | `bin/groundwork-run.js:2427` | sensitive-data-exposure
   resolveZcodeCoordinatorHome() materializes the user's full ~/.zcode/cli/config.json — which per the adjacent comment can contain inline provider API keys — into a mkdtemp dir under $TMPDIR. The file is 0600 inside 0700 directories, but cleanup only happens in invokePhase's finally; a killed/crashed runner leaves the credential copy in tmp until OS cleanup (about 3 days on macOS), duplicating secrets outside the location the user chose to keep them.
   *Recommendation:* Strip or redact provider credential material when only the model selection needs to change (e.g. rewrite provider entries to reference the real config's auth without copying key material), or register crash-safe cleanup (signal handlers plus a startup sweep of stale zcode-home-* dirs older than the max phase duration).

47. [ ] **security-reviewer** | `hooks/pin-session-selection.sh:34` | defense-in-depth
   The pin hook's command gate is a substring match on the executed command, so an injected command like `echo persist-project.js; cat receipt.json` passes the gate, and validateSelectionReceipt (lib/project-context.js:1032) only format-checks selection_id (any 24-hex string) — the receipt is not cryptographically bound to a real selector invocation. Impact is bounded: the forged receipt must still name a project configured in this repo's .groundwork.yml and a contained path, which is the same authority the model already has by legitimately invoking the selector, so this is a trust-model weakening rather than a privilege escalation.
   *Recommendation:* Have the selector also write the minted selection_id to a harness-state side record at selection time, and have the pin hook require the receipt's selection_id to match that record (or sign the receipt with a key only the selector holds) before pinning.

48. [ ] **security-reviewer** | `lib/atomic-write.js:40` | secure-storage
   writeFileSyncAtomic() fsyncs the temporary file but never fsyncs the parent directory after renameSync, so a machine crash can lose the rename itself. lease-mutation's publishRecordAtomically does fsync the directory (lib/lease-mutation.js:108), so the task's 'durable atomic writes' assumption holds for lease publication but not for .validation-session.json state writes, unworked-findings reports, or checkpoint writes that go through this helper — a post-crash reader may observe a stale-but-complete prior revision (safe) or the file's absence.
   *Recommendation:* Add an optional (default-on) directory fsync after the rename in writeFileSyncAtomic, mirroring fsyncDirectory, or document which callers tolerate rename loss.

49. [ ] **security-reviewer** | `lib/lease-mutation.js:223` | availability
   mutationEntries() throws on ANY directory entry not matching ^[0-9a-f]{48}\.lock$ (and the parallel checks in bin/groundwork-run.js liveLeaseFiles line 1039 and activeProjectOwners line 1088). A single benign stray file (.DS_Store from opening the .git/groundwork tree in Finder, an editor temp file) permanently wedges every subsequent lock acquisition and gate scan for that queue with a fatal error until the file is manually removed. Fail-closed is the right call for pattern-matching-but-malformed lease records, but it converts unrelated dirt into a self-DoS of the whole coordination layer.
   *Recommendation:* Distinguish clearly-non-lease names (dotfiles, non-hex names) — skip with a one-line warning — from names that claim to be lease/entry records but fail validation, which should keep failing closed.

50. [ ] **security-reviewer** | `lib/owned-lock.js:140` | stale-recovery
   holderAgeMs() prefers the holder-recorded acquiredAt timestamp over the file mtime. A lock record with a corrupt or hostile future acquiredAt yields a negative age, so the age-based stale path never fires and the slot is wedged behind a never-aging lock until manual removal (the dead-holder identity path still works when the record names a dead local pid, but an unreadable/foreign record with a bad timestamp blocks forever).
   *Recommendation:* Fall back to (or clamp with) the file's mtime whenever recordedAt is missing or implausibly in the future beyond a small clock-skew allowance.

51. [ ] **security-reviewer** | `lib/process-identity.js:65` | identity-spoofing
   When /proc is absent and `ps -o lstart=` fails while kill(pid,0) succeeds, processStartIdentity() degrades to the bare `pid:<pid>` string. That identity is not stable across pid recycling, so a reused pid is accepted as the original holder/heartbeat worker — precisely the spoof the module exists to prevent. This requires ps to be unavailable while the process is alive, so it is an edge-case degradation, and heartbeatWorkerAlive still gates the registered-worker path.
   *Recommendation:* Return null (undecidable, never reap) instead of a bare-pid identity, or include a monotonic marker (e.g. boot-estimate from sysctl kern.boottime) in the fallback so recycled pids cannot match.

52. [ ] **security-reviewer** | `lib/process-identity.js:84` | process-identity misparse
   Pre-existing at HEAD and semantically untouched by the repair: the /proc/<pid>/stat starttime is read as fields[21] from a naive whitespace split. A comm containing spaces shifts the index so fields[21] lands on another (often numeric, e.g. itrealvalue) field, yielding a stable-but-wrong identity such as "proc:0". In the false-dead direction, a live holder that rewrote its own comm (setproctitle) after recording its lease identity would probe as mismatched, risking reaping a live holder's lease/queue entry — the exact race the mutation queue exists to close. The macOS/BSD ps path (the primary platform per AGENTS.md) is robust; the numeric guard /^\d+$/ makes the misparse rare but not impossible on Linux.
   *Recommendation:* Same lastIndexOf(')') parse as finding 1; optionally assert that fields[2] is a single alpha state token before trusting fields[21].

53. [ ] **security-reviewer** | `lib/process-identity.js:87` | liveness-probe robustness
   The repair folded zombie detection into the naive whitespace-split /proc/<pid>/stat parse (field index 2). /proc stat field 2 (comm) is parenthesized and may contain spaces; when it does, every subsequent field index shifts, so fields[2] is a comm fragment and the /^Z/ test cannot match. A Linux zombie whose comm contains whitespace is then reported with a starttime identity instead of null (false-live), which can stall a queue waiter whose backoff loop has no deadline. Integrity is unaffected (no false-dead direction here); the old code detected zombies via a separate `ps -o stat=` call that was immune to comm spacing.
   *Recommendation:* Parse /proc/<pid>/stat by splitting after the last ')' (idx = statText.lastIndexOf(')'); fields after that point are state, ppid, ..., starttime with stable indices. This fixes both the new zombie check and the pre-existing starttime indexing (finding 2).

54. [ ] **security-reviewer** | `lib/project-context.js:1097` | path-traversal
   restoreSessionSelection() builds the snapshot path from an unvalidated sessionId (`${sessionId}__${slug}.json`), and getPaneKey()'s session fallback (line 733) likewise interpolates the raw GROUNDWORK_SESSION_ID into a filesystem name, while the write paths (persistSessionSelection, pinSessionSelection) enforce isSafeIdentifier. A traversal-shaped session id (e.g. containing ../) would read a selection JSON outside the chat-snapshots/panes directory and then steer project resolution from its {project, root} fields. Session ids come from the trusted harness in the declared threat model, so this is a read-side hardening gap, not an exploitable flaw.
   *Recommendation:* Apply the same isSafeIdentifier guard at the top of restoreSessionSelection and in getPaneKey's session fallback (return null / fall through to the repo-key fallback when it fails), making the read path symmetric with the write path.

55. [ ] **security-reviewer** | `lib/validation-session.js:732` | input-validation
   loadRunnerCapability() reads the runner's bearer-capability store with a bare fs.readFileSync — no O_NOFOLLOW, no lstat regular-file check, and no 0600 permission check — even though capabilityFromFile() (the manual counterpart reading the same ownerToken material) enforces all three. A symlink or substituted file at runner-capability.json inside the slot directory would be followed; this requires same-user write access to the 0700 slot dir, so it is an inconsistent-hardening gap rather than a live exploit.
   *Recommendation:* Route loadRunnerCapability through the same open(O_NOFOLLOW)+fstat+mode-check discipline as capabilityFromFile, rejecting non-regular, group/world-readable, or symlinked stores.

56. [ ] **spec-alignment-checker** | `bin/groundwork-run.js:1010` | specification-gap
   Queue sharding sets the project-lease mutation root to projects/<key>.queue, but a pre-sharding v2 runner of the same release line serializes the SAME projects/<key>.lock file through the old shared projects/.lease-mutation queue, so a mixed point-version fleet loses the single-queue serialization this task exists to guarantee (createOwnedLease publish, release, and reclaimStaleLease compare-and-unlink windows can interleave across the two queues). The code comment says 'drain old runners before upgrading, exactly like the repository-gate v2 precondition', but unlike the v1 boundary there is no detection: requireDrainedLegacyRunner only inspects groundwork/runner.lock, which v2 runners never write, so an old-v2 runner leaves no observable record and the degraded mode is silent. The frozen spec only mandates predecessor-version detection, so this is an operational gap in the hardening goal rather than a spec violation.
   *Recommendation:* Make the drain precondition observable: write a sharding/protocol version marker into groundwork/projects/ that acquireProjectLease checks (failing closed or warning on an old-queue record from a live runner), or document the same-point-version drain requirement in the README/AGENTS upgrade notes alongside the v2 boundary.

57. [ ] **spec-alignment-checker** | `bin/groundwork-run.js:3253` | specification-gap
   Frozen architecture.md ('Parallel task-runner synchronization') states 'token-named reader leases admit concurrent model phases' and 'then gives phase agents reader access', but this change deliberately removes the repository reader gate previously held across a phase (invokeOnce now calls callPhase without acquireRepositoryGate; tests/groundwork-run.test.js:1831 fails if a phase waits on the gate). The frozen product spec remains satisfied — writer exclusivity, per-project serialization, writer-priority-over-new-readers (acquireRepositoryGate('read') still yields to writers-waiting at the startup/setup/workspace call sites), and cross-project phase overlap all hold — so this is descriptive drift between the frozen architecture document and the hardened implementation, recorded so a later architecture sync does not reintroduce a phase-wide reader gate against the change's intent.
   *Recommendation:* When specs/architecture are next unfrozen, update the synchronization section to state that phases run un-gated inside their task worktrees and that reader leases are held only for startup, worktree setup, and other base-checkout-touching operations; no code change needed now.

58. [ ] **spec-alignment-checker** | `lib/owned-lock.js:158` | task-goal
   The 'foreign-host-unsupported' refusal text tells the operator 'a foreign-host lock cannot be probed or reaped from this host — never-reap applies ... remove it manually', but acquireOwnedLockTurn (lines 241-251) reaps ANY holder once holderAgeMs exceeds staleMs — including a foreign-host holder, since holderProvablyDead returning false is bypassed by the agedOut branch. The user-facing recovery contract the error states (never reap; manual removal only) does not match what the code does (silent age-based reap after 5 minutes). Related to already-ledgered unworked item #10 (cross-host mutual-exclusion hazard), but this finding is the specific message-vs-behavior contradiction in the new module's actionable refusal semantics.
   *Recommendation:* Either exclude foreign-host holders from the agedOut reap so the promised never-reap contract holds, or reword the message to state that a foreign-host lock is reclaimed after the staleness window (with the window named), keeping the manual-removal advice accurate.

59. [ ] **spec-alignment-checker** | `lib/validation-session.js:1227` | task-goal
   The command-line capability guard rejects only the hyphen spellings ('--owner-token' and '--owner-token='), but the underscore spelling '--owner_token <token>' (and '--owner_token=<token>') is parsed as an inert argument and silently ignored instead of rejected (verified: parseArgs(['heartbeat-beat','--owner_token','abc']) returns {owner_token:'abc'} without error). The declared transport contract ('the capability must never appear on the command line ... the CLI rejects --owner-token in either spelling', references/validation-session-protocol.md) therefore has a hole: a bearer token can still travel via argv — visible to every process through ps(1) — without triggering the documented refusal. The token is never authenticated in that form (no bypass), and the CLI rejection test (tests/validation-session.test.js:1367) covers only the hyphen spellings.
   *Recommendation:* In parseArgs, reject any argument whose key normalizes to the owner capability in either separator spelling (match /^--owner[-_]token(=|$)/), and extend the rejection test to cover the underscore form.

60. [ ] **spec-alignment-checker** | `skills/swarm-debug/SKILL.md:232` | specification-gap
   The Swarm Extension section still prescribes the journal format '{{debug_dir}}/${SLUG}-<timestamp>-<pid>.md', contradicting the invocation-unique mktemp run-directory contract implemented by this change (skills/debug/SKILL.md Phase 1 uses RUN_DIR="$(mktemp -d "{{debug_dir}}/${SLUG}-XXXXXX")"; JOURNAL_PATH="$RUN_DIR/journal.md", and swarm-debug's own Phase 1 paragraph at line 85 repeats it). docs/developing-skills.md invariant 1 explicitly bans 'a timestamp+PID-only contract (same-second and PID-reuse collisions)', so a follower of line 232 would recreate exactly the collision class the task's 'unique journal directories' item exists to remove.
   *Recommendation:* Rewrite line 232 to reference the mktemp-directory journal format ('$RUN_DIR/journal.md' from the shared Phase 1 procedure) instead of '${SLUG}-<timestamp>-<pid>.md'.

61. [ ] **spec-alignment-checker** | `specs/product_specs.md:7` | specification-gap
   Line 7 retains "Repository-wide mutations—including worktree creation/removal, recovery, merge publication, and branch cleanup—SHALL be exclusive," listing recovery among writer-exclusive repository-wide mutations. Corrected line 37 states recovery runs "with no repository gate held across it" under the per-task project lease, the README delta states "model and repair phases run under the per-task project lease," and architecture.md's rewritten paragraph says a phase (recovery included) never holds the repository gate. The enumeration item is stale remnant of the removed model and now contradicts the corrected text; the sentence's normative core (writer exclusivity for repository-wide mutations) remains true and implemented.
   *Recommendation:* Drop "recovery" from the exclusive-mutation enumeration in line 7, leaving worktree creation/removal, merge publication, and branch cleanup (all genuinely writer-gated per bin/groundwork-run.js invokeOnce comment and call-site gating).

62. [ ] **test-quality-reviewer** | `lib/atomic-write.js:65` | missing-test
   publishFileSyncExclusive — the exclusive no-clobber publication used by the run-keyed unworked-findings path — has no test in tests/atomic-write.test.js. Its EEXIST collision contract, temporary cleanup on collision, 0600 mode, and mkdir option are only exercised indirectly through lib/persist-unworked-findings.js integration tests, so a regression localized to this helper (e.g., temp file leaked on EEXIST, or clobbering rename instead of link) would surface only as an unrelated persistence failure.
   *Recommendation:* Add unit cases to tests/atomic-write.test.js: publish succeeds once and returns the path; a second publish to the same destination throws EEXIST and leaves the original content and no .tmp leftovers; mode is 0600; destination directory listing stays clean.

63. [ ] **test-quality-reviewer** | `lib/validation-session.js:246` | missing-test
   inspectActiveValidationSession is exported and consumed by bin/groundwork-run.js (lines 1351, 3602) but has no direct test for its distinct behaviors: returning null on absent pointer, returning null on identity mismatch, and throwing 'incomplete validation session task HEAD changed' when the worktree HEAD moved under an unvalidated session. Only the happy inspection path is covered transitively via the runner's crash-resume orchestration test (tests/groundwork-run.test.js:4795+).
   *Recommendation:* Add a small describe block in tests/validation-session.test.js covering the three inspection outcomes, especially the HEAD-changed fail-closed throw, which is a state-integrity guarantee of the durable session machine.

64. [ ] **test-quality-reviewer** | `tests/install-config.test.js:1972` | slow-test
   Suite runtime is dominated by repeated heavyweight setup: fixtureSourceCorruption copies the entire 3.2MB plugin tree per scenario (~20 invocations inside the closure-bypass matrix alone), the parity matrix in tests/groundwork-run.test.js:7055+ runs the full bash installer once per config label (~10 installs) plus the manifest-fixture test installs twice more, the 100-round race soak (tests/owned-lock.test.js:331) spawns ~200 node processes, and two sleep-window negative assertions add fixed ~6.2s (tests/validation-session.test.js:1280 sleepMs(5200); tests/groundwork-run.test.js:7137 sleepTest(1000)). None are flaky (the sleep windows fail-open, not fail-closed), but they push the plain-Node suite to the reported ~15 minutes.
   *Recommendation:* Build one corrupted source base per matrix and mutate files in place per case; run the installer once per parity suite and reuse the installed tree across config labels (the installed runner is config-independent); default GROUNDWORK_RACE_REPS lower in CI with an env override for soak runs; consider shrinking the 5.2s contention window via the existing GROUNDWORK_MUTATION_LOCK_WAIT_MS env used elsewhere in the same test.

65. [ ] **test-quality-reviewer** | `tests/install-config.test.js:2110` | multiple-concerns
   'parenthesized and comment-obscured requires cannot bypass closure validation' packs roughly twenty labeled scenarios (parenthesized, comment-obscured, template-substitution, optional-call, alias forms, Reflect.apply, classified passes, unterminated lexemes, prose strings) into one test function with five sequential for-loops. A failure reports one test name for any of ~20 behaviors, hurting failure localization — inconsistent with the repo's own generated-test pattern used for the config parity matrix and the invalid-config table.
   *Recommendation:* Hoist the case tables to describe scope and generate one test() per labeled case (the codebase already does this at tests/project-context.test.js:780 and tests/groundwork-run.test.js:7055), preserving the shared fixture helper.

66. [ ] **test-quality-reviewer** | `tests/lease-mutation.test.js:147` | test-isolation
   The symlinked-entry case materializes its symlink target at os.tmpdir()/gw-lease-mutation-real-<pid>.json, outside the per-test fixture root, so it is never cleaned up and accumulates one stray file per pid across runs. Pure hygiene: determinism, assertions, and queue-state pinning are unaffected.
   *Recommendation:* Create the symlink target inside the per-test tmp() directory (a sibling of fixture.parent) so the existing finally rmSync removes it with the rest of the fixture.

67. [ ] **test-quality-reviewer** | `tests/owned-lock.test.js:366` | implementation-coupling
   Several new tests assert on source text rather than behavior: 'protocol wiring' greps owned-lock.js/validation-session.js for 'lease-mutation' and absence of 'openSync(lockFile, 'wx'' (tests/owned-lock.test.js:366-386); 'the capability is never read from the environment' greps validation-session.js (tests/validation-session.test.js:1567); activeProjectOwners source-slice greps forbid '`task/${' (tests/groundwork-run.test.js:1450-1456); 'the pin hook never consults shared pane state' greps pin-session-selection.sh (tests/project-context.test.js:1370); pi TS-delegation greps (tests/pi-extension.test.js:256-271). Each is supplementary to strong behavioral coverage, but they fail on benign refactors (renamed helper, reworded comment) while being bypassable — maintenance cost with partial protection.
   *Recommendation:* Keep at most one policy-anchor grep per invariant and mark it as such, or convert to behavior where cheap (e.g., env-transport can be asserted behaviorally by running the CLI with the token in env and expecting failure; activeProjectOwners branch reconstruction is already pinned by the factory-delegation assertions above it).

68. [ ] **test-quality-reviewer** | `tests/pi-extension.test.js:263` | logic-in-test
   Dead always-true assertion clause: `assert.ok(!/name:\s*\\?/.test('') && !projectContext.includes('- name:'))` — the first operand tests an empty string and can never fail, obscuring what is actually being asserted.
   *Recommendation:* Delete the `!/name:\s*\\?/.test('') &&` clause, leaving the meaningful `!projectContext.includes('- name:')` assertion.

69. [ ] **test-quality-reviewer** | `tests/validation-lock.test.js:292` | redundant-test
   The wrong/forged-capability resume refusal is pinned twice across suites: 'a wrong capability cannot resume a live session' here, and again in tests/validation-session.test.js ('a non-owner capability is rejected by every mutating operation' line 670+, 'fixer-inflight recovery...' line 925, and 'a crashed heartbeat worker...' line 825). The overlap adds maintenance cost (fixture duplication across two files) without catching additional defects; the validation-session variants are strictly broader.
   *Recommendation:* Differentiate or consolidate: keep validation-lock.test.js focused on lockfile mechanics and drop its capability-forgery case, or cross-reference the two so a future contract change updates one site.

70. [ ] **test-quality-reviewer** | `tests/validation-session.test.js:1339` | non-deterministic
   The host-identity liveness table asserts that pid 424242 is dead under the real probe (sessionLiveness expects 'stale'). On Linux hosts with pid_max above 424242 (supported range reaches 4194304) that pid can genuinely exist, making the test flaky there. Note tests/validation-lock.test.js avoids this correctly by injecting the probe shim; only this direct-probe assertion depends on the assumption.
   *Recommendation:* Replace the magic pid with a provably dead one: spawn a child, SIGKILL and wait for exit, then use its pid; or assert via the injectable dependencies.processStartIdentity seam as validation-lock.test.js does.
